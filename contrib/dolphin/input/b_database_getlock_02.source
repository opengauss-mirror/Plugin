drop database if exists b_databaselock2;
create database b_databaselock2 dbcompatibility 'b';
\c b_databaselock2

--all succeed
select get_lock('`');
select get_lock('~');
select get_lock('!');
select get_lock('@');
select get_lock('#');
select get_lock('$');
select get_lock('%');
select get_lock('^');
select get_lock('&');
select get_lock('*');
select get_lock('(');
select get_lock(')');
select get_lock('[');
select get_lock(']');
select get_lock('{');
select get_lock('}');
select get_lock('-');
select get_lock('_');
select get_lock('=');
select get_lock('+');
select get_lock('\');
select get_lock('|');
select get_lock(';');
select get_lock(':');
select get_lock('""""');
select get_lock(',');
select get_lock('.');
select get_lock('/');
select get_lock('<');
select get_lock('>');
select get_lock('?');

select get_lock('`',1);
select get_lock('~',1);
select get_lock('!',1);
select get_lock('@',1);
select get_lock('#',1);
select get_lock('$',1);
select get_lock('%',1);
select get_lock('^',1);
select get_lock('&',1);
select get_lock('*',1);
select get_lock('(',1);
select get_lock(')',1);
select get_lock('[',1);
select get_lock(']',1);
select get_lock('{',1);
select get_lock('}',1);
select get_lock('-',1);
select get_lock('_',1);
select get_lock('=',1);
select get_lock('+',1);
select get_lock('\',1);
select get_lock('|',1);
select get_lock(';',1);
select get_lock(':',1);
select get_lock('""""',1);
select get_lock(',',1);
select get_lock('.',1);
select get_lock('/',1);
select get_lock('<',1);
select get_lock('>',1);
select get_lock('?',1);

select get_lock('·');
select get_lock('~');
select get_lock('！');
select get_lock('@');
select get_lock('#');
select get_lock('￥');
select get_lock('%');
select get_lock('……');
select get_lock('&');
select get_lock('*');
select get_lock('（');
select get_lock('）');
select get_lock('-');
select get_lock('——');
select get_lock('=');
select get_lock('+');
select get_lock('【');
select get_lock('】');
select get_lock('{');
select get_lock('}');
select get_lock('、');
select get_lock('|');
select get_lock('；');
select get_lock('：');
select get_lock('‘');
select get_lock('“');
select get_lock('”');
select get_lock('，');
select get_lock('《');
select get_lock('。');
select get_lock('》');
select get_lock('/');
select get_lock('？');

select get_lock('·',1);
select get_lock('~',1);
select get_lock('！',1);
select get_lock('@',1);
select get_lock('#',1);
select get_lock('￥',1);
select get_lock('%',1);
select get_lock('……',1);
select get_lock('&',1);
select get_lock('*',1);
select get_lock('（',1);
select get_lock('）',1);
select get_lock('-',1);
select get_lock('——',1);
select get_lock('=',1);
select get_lock('+',1);
select get_lock('【',1);
select get_lock('】',1);
select get_lock('{',1);
select get_lock('}',1);
select get_lock('、',1);
select get_lock('|',1);
select get_lock('；',1);
select get_lock('：',1);
select get_lock('‘',1);
select get_lock('“',1);
select get_lock('”',1);
select get_lock('，',1);
select get_lock('《',1);
select get_lock('。',1);
select get_lock('》',1);
select get_lock('/',1);
select get_lock('？',1);
\c postgres
drop database if exists b_databaselock2;
