# The loader regression verifies the per-role gs_role_copy_files gate.  The
# fixed openGauss Server defaults this postmaster-level gate to off, so enable
# it only inside the disposable regression cluster.
enable_copy_server_files = on
